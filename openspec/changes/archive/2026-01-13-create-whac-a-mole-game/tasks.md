# Tasks

1.  **Project Setup**
    - [ ] Initialize Vite + React project.
    - [ ] Install Tailwind CSS and configure `tailwind.config.js`.
    - [ ] Install `react-router-dom`.
    - [ ] Update `vite.config.js` with `base: '/whac-a-mole/'`.
    - [ ] Update `main.jsx` with `HashRouter`.

2.  **Core Components & Routing**
    - [ ] Create `App.jsx` with layout and Routes.
    - [ ] Create `pages/Home/index.jsx` with "Start Game" button.
    - [ ] Create `pages/Result/index.jsx` with "Play Again" button.

3.  **Game Implementation**
    - [ ] Create `pages/Game/index.jsx` grid layout.
    - [ ] Implement game loop (timer, mole appearance).
    - [ ] Implement scoring logic (positive feedback).
    - [ ] Implement settings toggle (Speed / Size).

4.  **Accessibility & Polish**
    - [ ] Apply `touch-action: manipulation` global style.
    - [ ] Implement "forgiving hitbox" (padding around mole).
    - [ ] Verify `h-dvh` behavior on mobile.
